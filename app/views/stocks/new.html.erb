<%= simple_form_for [@frigo, @stock] do |f| %>
  <%= f.input :quantity %>
  <%= f.input :ingredient, collection: Ingredient.all %>
  <%= f.submit "Add the item to the fridge", class: "btn btn-primary" %>
<% end %>
